import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#5173EB",
    },
    secondary: {
      main: "#5A5E6B",
    },
    error: {
      main: "#EB5076",
    },
  },
});
